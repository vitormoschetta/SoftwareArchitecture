using System;

namespace Problem.CreditProtectors
{
    public class SPC
    {
        public bool Consulta(string cpf)
        {
            Console.WriteLine("SPC - consultando CPF {0}...", cpf);
            return false;
        }
    }
}